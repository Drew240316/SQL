mysql은 date타입을 문자열로 자동변환해서 특정 글자를 찾을 수 있다.

★ 정답) year + count
SELECT COUNT(*) AS FISH_COUNT
FROM FISH_INFO
WHERE YEAR(TIME) = 2021;

★ 정답) like + count
SELECT COUNT(*) AS FISH_COUNT
FROM FISH_INFO
WHERE TIME LIKE '2021%';

★ 정답) extract + count
SELECT COUNT(*) AS FISH_COUNT
FROM FISH_INFO
WHERE EXTRACT(YEAR FROM TIME) = 2021;

★ 정답) between + count
SELECT COUNT(*) AS FISH_COUNT
FROM FISH_INFO
WHERE TIME BETWEEN '2021-01-01' AND '2021-12-31';

★ 정답) extract + count 
SELECT COUNT(*) AS FISH_COUNT
FROM FISH_INFO
WHERE EXTRACT(YEAR FROM TIME) = 2021;

★ 정답) left + count 
SELECT COUNT(*) AS FISH_COUNT
FROM fish_info
WHERE LEFT(time, 4) = '2021';

★ 정답) substirng + count 
SELECT COUNT(*) AS FISH_COUNT
FROM fish_info
WHERE SUBSTRING(time, 1, 4) = '2021';

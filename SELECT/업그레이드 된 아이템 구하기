select a.item_id, a.item_name, a.rarity
from item_info a join item_tree b 
on a.item_id = b.item_id
where b.PARENT_ITEM_ID in (
    select item_id
    from item_info
    where rarity = 'RARE')
order by item_id desc
;

-- 쿼리 실행 순서
-- from > join on > where(서브쿼리 결과) > where 결과에 따른 조건 적용 > group by > having > select > order by > limit 

-- 아이템의 희귀도가 rare인 : 0 1 3 4
-- 모든 다음 업그레이드 : item_tree테이블에서 parent_id로 확인 가능
-- 아이템 : item_tree테이블의 item_id

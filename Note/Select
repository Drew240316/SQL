★ 그룹바이를 쓸 때, SELECT 절에서 허용되는 조합
  SELECT 안에 들어가도 되는 것 4가지
  - 그룹 기준 컬럼(GROUP BY 컬럼)
  - 집계 함수(MAX, SUM, COUNT 등)
  - 집계 결과를 가공하는 표현식 (CASE WHEN + 집계 함수)
  - 집계 결과를 조건으로 쓰는 서브쿼리

★ 그룹바이를 쓸 때, SELECT 안에 넣으면 안 되는 것 
  - 집계가 아닌 일반 컬럼 (GROUP BY에 없는 컬럼)
  - 일반 컬럼만 사용하는 CASE WHEN
  - 일반 컬럼만 사용하는 서브쿼리

문자열 길이
LENGTH('hello') → 5

소문자/대문자 변경
LOWER('HELLO') → hello
UPPER('hello') → HELLO

문자열 자르기
SUBSTRING('abcdef', 2, 3) → 'bcd'

문자열 합치기
CONCAT('a', 'b') → 'ab'

특정 문자열 포함 여부
INSTR('banana', 'na') → 3 (첫 등장 위치)

문자열 바꾸기
REPLACE('a-b-c', '-', '/') → 'a/b/c'

양쪽 공백 제거
TRIM('  hi  ') → 'hi'

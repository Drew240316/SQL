where절의 in 연산자는 여러 값 중 하나와 일치하면 true가 된다.
호환은 exist , or이다.
단, exist는 행이 t/f인지 반환하고, in은 리스트를 반환하는 형태

★ 문법
where 값/컬럼명 in (값1, 값2, 값3) : 값 1 2 3 속하는 것 중 1개
where 값/컬럼명 not in (값1, 값2, 값3) : 값 1 2 3을 제외한 나머지
where 값/컬럼명 in (다중행 서브쿼리) : 다중행으로 나온 값들을 사용
where 컬럼명 in (상관 서브쿼리) : 나오는 행마다 where절의 컬럼과 계속 한 행씩 비교 한다. 그래서 서브쿼리 안에 참조가 꼭 있음.


★ 문제의 표현
~인 경우만, ~중에서, ~에 해당하는 데이터만, 특정 (지역) 들만 조회, ~의 리스트만,
~ 을 제외하고(not in), 다음 값들 중 하나인 경우


